{
  "name": "mongodb-project",
  "version": "1.0.0",
  "description": "testing mongoDB features with backend server",
  "main": "index.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "npx prettier --check --cache server/**/*.js",
    "lint": "npx eslint --cache server/**/*.js",
    "format": "npx prettier --check --write --cache server/**/*.js && npx eslint --fix --cache server/**/*.js",
    "dev": "npm run format && nodemon index",
    "start": "npm run format && node index"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KostiantynO/mongoDB-project.git"
  },
  "keywords": [],
  "author": "Kostiantyn Ochenash <ochenashk@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/KostiantynO/mongoDB-project/issues"
  },
  "homepage": "https://github.com/KostiantynO/mongoDB-project#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "joi": "^17.6.0",
    "mongoose": "^6.4.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.18.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^8.5.0",
    "nodemon": "^2.0.18",
    "prettier": "^2.7.1"
  }
}
