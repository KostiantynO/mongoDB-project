{
  "name": "mongodb-project",
  "version": "1.0.0",
  "description": "testing mongoDB features with backend server",
  "main": "src/app.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "npx prettier --check --cache src/**/*.js",
    "lint": "npx eslint --cache src/**/*.js",
    "format": "npx prettier --check --write --cache src/**/*.js && npx eslint --fix --cache src/**/*.js",
    "dev": "npm run lint && nodemon src/app",
    "start": "npm run lint && node src/app"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KostiantynO/mongoDB-project.git"
  },
  "keywords": [],
  "author": "Kostiantyn Ochenash <ochenashk@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/KostiantynO/mongoDB-project/issues"
  },
  "homepage": "https://github.com/KostiantynO/mongoDB-project#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "joi": "^17.6.0",
    "mongoose": "^6.4.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.18.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-node": "^11.1.0",
    "nodemon": "^2.0.18",
    "prettier": "^2.7.1"
  }
}
